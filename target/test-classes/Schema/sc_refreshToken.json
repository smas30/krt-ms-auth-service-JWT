{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Schema de validacion para respuesta de refreshToken",
  "description": "Esquema que valida la estructura de respuesta del servicio de refreshToken JWT",
  "type": "object",
  "properties": {
    "token": {
      "type": "string",
      "description": "Token JWT refrescado con formato estandar",
      "pattern": "^[A-Za-z0-9-_=]+\\.[A-Za-z0-9-_=]+\\.[A-Za-z0-9-_=]+$",
      "minLength": 10
    }
  },
  "required": ["token"],
  "additionalProperties": false
} 